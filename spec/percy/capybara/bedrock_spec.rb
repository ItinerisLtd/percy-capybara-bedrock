RSpec.describe Percy::Capybara::Bedrock do
  it "has a version number" do
    expect(Percy::Capybara::Bedrock::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
